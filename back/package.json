{
  "name": "test-back",
  "version": "0.0.1",
  "main": "server.js",
  "dependencies": {
    "body-parser": "^1.12.4",
    "express": "^4.12.4"
  },
  "scripts": {
    "init-project": "npm install npm -g && npm install -g bower && npm i && cd ../front && npm i && bower i"
  }
}
